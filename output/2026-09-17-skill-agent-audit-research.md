# .claude 스킬·에이전트 개선 및 재평가 결과

- 조사일: 2026-09-17
- 조사 도구: `.claude/skills`, `.claude/agents`, `CLAUDE.md`, `AGENTS.md`, `output/` 실제 산출물 직접 검토
- 문서 성격: 내부 참고용 감사(audit) 기록. 발행용 콘텐츠 아님.

---

## 1. 이전 평가 (개선 전) — 65/100

| 항목 | 배점 | 점수 |
|---|---:|---:|
| A. 라우팅·모델 배정·문서 정합성 | 25 | 15 |
| B. 사실검증·비방금지 등 안전장치 실효성 | 25 | 10 |
| C. 저장 규칙(경로·파일명) 일관성 | 15 | 13 |
| D. 역할 경계·중복 방지 | 15 | 12 |
| E. 스킬 파일 자체 완성도 | 10 | 7 |
| F. 실제 output 산출물과의 정합성 | 10 | 8 |
| **합계** | **100** | **65** |

---

## 2. 적용한 개선 사항

| 파일 | 변경 내용 | 대응 항목 |
|---|---|---|
| `.claude/agents/blog-writer.md` → `blog-writer-research.md` | 파일명·`name` frontmatter를 `blog-writer-research`로 변경, 설명에 스킬 `blog-writer`와 다르다는 안내 추가 | A, D (이름 중복 해소) |
| `CLAUDE.md` | 라우팅 규칙의 에이전트명 갱신, 모델 배정 표에 `blog-writer-research`·`beauty-writer` 추가, 예시 파일명 오타(`20206`→`2026`) 수정 | A |
| `AGENTS.md` | `CLAUDE.md`와 완전히 동기화 (editor·publisher·blog-writer-research 라우팅/모델 배정 누락분 추가), 상단에 "CLAUDE.md와 동일하게 유지" 안내 추가 | A |
| `.claude/skills/seo-checklist/SKILL.md` | 항목 8·9(출처 없는 통계, 비방·과장 표현)를 즉시 반려 조건으로 추가, "핵심 키워드"·"키워드 밀도" 판정 기준 명문화, 결과 보고 형식 추가 | B, E |
| `.claude/agents/researcher.md` | 출처 표기에 "미확인"/URL 외 임의 라벨("추정"·"관측값" 등) 금지 규칙 추가, 계산값 처리 방법 명시 | B |
| `.claude/skills/ad-copy`, `blog-writer`, `email-newsletter`, `sns-caption` (SKILL.md 4종) | 각 스킬에 "검증되지 않은 통계 인용 금지·경쟁사 비방 금지" 및 저장 경로/파일명 규칙을 직접 명시(기존엔 CLAUDE.md 참조에만 의존) | B, C, E |
| `.claude/agents/publisher.md` | `blog/articles` 파일명 규칙이 CLAUDE.md와 별개인 publisher 전용 규칙임을 명시 | C |

---

## 3. 재평가 — 94/100

| 항목 | 배점 | 점수 | 근거 |
|---|---:|---:|---|
| A. 라우팅·모델 배정·문서 정합성 | 25 | **23** | 이름 중복·모델 배정 누락 해소. 잔여 감점(-2)은 `CLAUDE.md`/`AGENTS.md` 두 파일을 계속 수동으로 동기화해야 하는 구조적 위험(단일 소스가 아님)이 남아 있기 때문 |
| B. 사실검증·비방금지 등 안전장치 실효성 | 25 | **24** | seo-checklist 즉시반려 항목·researcher 라벨 규칙 추가로 대부분 해소. 잔여 감점(-1)은 출처 URL이 실제로 존재·정확한지 자동 검증할 수단이 없다는 구조적 한계(LLM 조사의 근본적 한계로, 규칙만으로 완전히 없앨 수 없음) |
| C. 저장 규칙(경로·파일명) 일관성 | 15 | **14** | 4개 콘텐츠 스킬에 저장 규칙 명문화, publisher 규칙 출처 명시로 해소. 잔여 감점(-1)은 문서화일 뿐 강제 검증 메커니즘은 없다는 점 |
| D. 역할 경계·중복 방지 | 15 | **15** | blog-writer 이름 중복이 유일한 감점 요인이었고 완전히 해소됨 |
| E. 스킬 파일 자체 완성도 | 10 | **10** | seo-checklist의 키워드 판정·밀도 계산·보고 형식 공백이 모두 채워짐 |
| F. 실제 output 산출물과의 정합성 | 10 | **8** | 규칙은 강화됐으나, 이 개선 이후 새로 생성된 산출물이 아직 없어 실제 준수 여부가 검증되지 않음. 또한 개선 전 만들어진 `output/2026-09-17-오너드채널마케팅-research.md`의 "(관측값)" 라벨은 소급 수정하지 않았으므로 과거 산출물엔 여전히 남아 있음 |
| **합계** | **100** | **94** | |

---

## 4. 남은 잔여 리스크 (구조적으로 100점이 될 수 없는 이유)

1. `CLAUDE.md`와 `AGENTS.md`는 여전히 별도 파일이라 수동 동기화가 필요하다. 한쪽만 고치고 잊으면 다시 어긋난다.
2. seo-checklist의 8·9번 항목은 "출처 없는 통계"를 판별하라고 지시하지만, 이 판별 자체는 실행하는 모델의 판단에 의존한다 — 외부 사실 확인 도구(웹 검색 재검증 등)를 강제하지는 않는다.
3. 기존 산출물(`2026-09-17-오너드채널마케팅-research.md`의 "관측값" 표기 등)은 새 규칙 적용 전에 만들어져 그대로 남아 있다. 필요하면 별도로 재작성 요청 필요.
